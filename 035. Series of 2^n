#include <stdio.h>
#include <math.h>

int main()
{
   unsigned int n, i;
   printf("\nEnter the value for n: ");
   scanf("%u", &n);
   
   for(i=0; i<=n; i++)
   {
       printf("%.2lf, ", pow(2,i));
   }

    return 0;
